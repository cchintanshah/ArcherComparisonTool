<Window x:Class="ArcherComparisonTool.WPF.Views.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Enter Password" 
        Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" x:Name="MessageTextBlock" TextWrapping="Wrap" Margin="0,0,0,15"/>
        
        <TextBlock Grid.Row="1" Text="Password:" Margin="0,0,0,5"/>
        <PasswordBox Grid.Row="2" x:Name="PasswordBox" Margin="0,0,0,15"/>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Click="OkButton_Click" Padding="20,8" Margin="5" Style="{StaticResource AccentButtonStyle}"/>
            <Button Content="Cancel" Click="CancelButton_Click" Padding="20,8" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
